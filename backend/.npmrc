# Don't install devDependencies in production unless explicitly needed
legacy-peer-deps=true
# Ignore scripts during install to avoid issues
ignore-scripts=false
# Use exact versions to avoid conflicts
save-exact=true