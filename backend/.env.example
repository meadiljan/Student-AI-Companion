# Example environment variables for the agent-task-api
# Copy this file to .env and fill in the values before starting the server.

# Server port
PORT=8787

# LLM API key used by the backend to call external LLM providers
# e.g. OpenAI API key, or any other provider key your app supports.
LLM_API_KEY=

# Optional: provider name (openai|gemini|groq)
LLM_PROVIDER=openai
